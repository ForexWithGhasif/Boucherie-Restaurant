import type {Metadata} from "next";

export const metadata:Metadata={title:"Book a Table | Boucherie",description:"Choose a Boucherie restaurant in New York, Chicago, Miami or Washington D.C. and continue to the official reservation experience."};

export default function ReservationsLayout({children}:{children:React.ReactNode}){return children}
